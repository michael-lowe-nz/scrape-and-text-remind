# AUTOMATICALLY GENERATED FILE, DO NOT EDIT MANUALLY.
# Generated by AWS CDK and [cdk-pipelines-github](https://github.com/cdklabs/cdk-pipelines-github)


name: Test and Deploy
on:
  push:
    branches:
      - main
      - chore/workflow-names
  workflow_dispatch: {}
jobs:
  Build-Test:
    name: Synthesize
    permissions:
      contents: read
      id-token: write
      checks: write
    runs-on: ubuntu-latest
    needs: []
    env:
      CONTACTS_YML: ${{ vars.CONTACTS_YML }}
      TEST_PHONE_NUMBER: ${{ vars.TEST_PHONE_NUMBER }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build
        run: |-
          yarn
          yarn build
      - name: Upload cdk.out
        uses: actions/upload-artifact@v3
        with:
          name: cdk.out
          path: cdk.out
      - name: Upload Test Reports
        uses: actions/upload-artifact@v3
        with:
          name: test-reports
          path: test-reports
      - name: Add Test Report to Pull Request
        if: success() || failure()
        uses: dorny/test-reporter@v1
        with:
          name: Jest Unit Tests
          path: test-reports/junit.xml
          reporter: jest-junit
  test-stage-TextReminders-Deploy:
    name: Deploy to Test
    permissions:
      contents: read
      id-token: write
    environment: Test
    needs:
      - Build-Test
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: Authenticate Via OIDC Role
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-region: ap-southeast-2
          role-duration-seconds: 1800
          role-skip-session-tagging: true
          role-to-assume: arn:aws:iam::746512892315:role/GitHubActionRole
      - name: Download cdk.out
        uses: actions/download-artifact@v3
        with:
          name: cdk.out
          path: cdk.out
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: "npm"
      - name: Install CDK dependencies from cache
        run: |-
          yarn
      - name: CDK deploy
        run: npx cdk deploy --app "cdk.out" --require-approval never "test-stage/*"
  qa-approval:
    name: "QA Engineer's Approval"
    environment: Test
    needs:
      - Build-Test
      - test-stage-TextReminders-Deploy
    runs-on: ubuntu-latest
    steps:
      - name: QA Approval
        run: echo "Waiting on QA approval"
  change-approval:
    name: "Change Control Officer's Approval"
    environment: Test
    needs:
      - Build-Test
      - test-stage-TextReminders-Deploy
    runs-on: ubuntu-latest
    steps:
      - name: QA Approval
        run: echo "Waiting on Change approval"
  prod-stage-TextReminders-Deploy:
    name: Deploy to Prod
    permissions:
      contents: read
      id-token: write
    environment: Production
    needs:
      - Build-Test
      - test-stage-TextReminders-Deploy
      - change-approval
      - qa-approval
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: Authenticate Via OIDC Role
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-region: ap-southeast-2
          role-duration-seconds: 1800
          role-skip-session-tagging: true
          role-to-assume: arn:aws:iam::746512892315:role/GitHubActionRole
      - name: Download cdk.out
        uses: actions/download-artifact@v3
        with:
          name: cdk.out
          path: cdk.out
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: "npm"
      - name: Install CDK dependencies from cache
        run: |-
          yarn
      - name: CDK deploy
        run: npx cdk deploy --app "cdk.out" --require-approval never "prod-stage/*"
