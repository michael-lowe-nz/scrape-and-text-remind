# AUTOMATICALLY GENERATED FILE, DO NOT EDIT MANUALLY.
# Generated by AWS CDK and [cdk-pipelines-github](https://github.com/cdklabs/cdk-pipelines-github)

name: deploy
on:
  push:
    branches:
      - main
  workflow_dispatch: {}
jobs:
  Build-Build:
    name: Synthesize
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    needs: []
    env:
      CONTACTS_YML: ${{ vars.CONTACTS_YML }}
      TEST_PHONE_NUMBER: ${{ vars.TEST_PHONE_NUMBER }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build
        run: |-
          yarn
          yarn build
      - name: Upload cdk.out
        uses: actions/upload-artifact@v3
        with:
          name: cdk.out
          path: cdk.out
      - name: Upload Test Reports
        uses: actions/upload-artifact@v3
        with:
          name: test-reports
          path: test-reports
  test-stage-TextReminders-Deploy:
    name: Deploy teststageTextRemindersCA2DC144
    permissions:
      contents: read
      id-token: write
    environment: Test
    needs:
      - Build-Build
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: Authenticate Via OIDC Role
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-region: ap-southeast-2
          role-duration-seconds: 1800
          role-skip-session-tagging: true
          role-to-assume: arn:aws:iam::746512892315:role/GitHubActionRole
      - name: Assume CDK Deploy Role
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-region: ap-southeast-2
          role-duration-seconds: 1800
          role-skip-session-tagging: true
          aws-access-key-id: ${{ env.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ env.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ env.AWS_SESSION_TOKEN }}
          role-to-assume: arn:aws:iam::653221278763:role/cdk-hnb659fds-deploy-role-653221278763-ap-southeast-2
          role-external-id: Pipeline
      - name: Download cdk.out
        uses: actions/download-artifact@v3
        with:
          name: cdk.out
          path: cdk.out
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: "npm"
      - name: Install CDK dependencies from cache
        run: |-
          yarn
      - name: CDK deploy
        run: npx cdk deploy --app "cdk.out" --require-approval never "test-stage/*"
  prod-stage-TextReminders-Deploy:
    name: Deploy prodstageTextReminders32176EAB
    permissions:
      contents: read
      id-token: write
    environment: Production
    needs:
      - Build-Build
      - test-stage-TextReminders-Deploy
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: Authenticate Via OIDC Role
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-region: ap-southeast-2
          role-duration-seconds: 1800
          role-skip-session-tagging: true
          role-to-assume: arn:aws:iam::746512892315:role/GitHubActionRole
      - name: Assume CDK Deploy Role
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-region: ap-southeast-2
          role-duration-seconds: 1800
          role-skip-session-tagging: true
          aws-access-key-id: ${{ env.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ env.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ env.AWS_SESSION_TOKEN }}
          role-to-assume: arn:aws:iam::365979456435:role/cdk-hnb659fds-deploy-role-365979456435-ap-southeast-2
          role-external-id: Pipeline
      - name: Download cdk.out
        uses: actions/download-artifact@v3
        with:
          name: cdk.out
          path: cdk.out
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: "npm"
      - name: Install CDK dependencies from cache
        run: |-
          yarn
      - name: CDK deploy
        run: npx cdk deploy --app "cdk.out" --require-approval never "prod-stage/*"
